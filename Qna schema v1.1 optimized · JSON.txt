{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harbourmaster.ai/schemas/qna_schema_v1.1.json",
  "title": "Harbourmaster Q&A Schema (GPT-o1-pro Optimized)",
  "description": "Schema for validated Q&A entries with domain-prefixed tags and 9-category structure",
  "version": "1.1",
  "lastUpdated": "2025-10-16",
  "type": "object",
  "additionalProperties": false,
  "required": ["harbour", "question", "answer", "category", "tags", "tier"],
  "properties": {
    "harbour": { 
      "type": "string", 
      "minLength": 2, 
      "maxLength": 100,
      "description": "Harbour name (e.g., 'Kioni', 'Vathi', 'Frikes')"
    },
    "question": { 
      "type": "string", 
      "minLength": 6,
      "description": "Clear question in natural language"
    },
    "answer": { 
      "type": "string", 
      "minLength": 6,
      "description": "Answer with numbered steps for Pro tier, ≤2 sentences for Free tier"
    },
    "category": {
      "type": "string",
      "enum": [
        "Approach & Entry",
        "Mooring",
        "Anchoring",
        "Weather & Shelter",
        "Safety & Hazards",
        "Facilities & Services",
        "Local Knowledge",
        "Media Tutorials",
        "General"
      ],
      "description": "Primary category from GPT-o1-pro optimized 9-category structure"
    },
    "tags": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "string",
        "pattern": "^(scope|mooring|anchor|weather|swell|surge|hazard|facility|atmosphere|crowding|timing|media):[a-z0-9_<>\\-]+$",
        "description": "Domain-prefixed tag"
      },
      "uniqueItems": true
    },
    "tier": { 
      "type": "string", 
      "enum": ["free", "pro", "exclusive"]
    },
    "notes": { 
      "type": ["string", "null"], 
      "maxLength": 500
    }
  },
  "allOf": [
    {
      "if": { "properties": { "category": { "const": "Mooring" } } },
      "then": { "properties": { "tags": { "contains": { "pattern": "^mooring:" } } } }
    },
    {
      "if": { "properties": { "category": { "const": "Anchoring" } } },
      "then": { "properties": { "tags": { "contains": { "pattern": "^anchor:" } } } }
    },
    {
      "if": { "properties": { "category": { "const": "Weather & Shelter" } } },
      "then": { "properties": { "tags": { "contains": { "pattern": "^(weather|swell|surge):" } } } }
    },
    {
      "if": { "properties": { "category": { "const": "Facilities & Services" } } },
      "then": { "properties": { "tags": { "contains": { "pattern": "^facility:" } } } }
    }
  ]
}